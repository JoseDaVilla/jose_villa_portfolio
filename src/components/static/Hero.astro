---
// src/components/static/Hero.astro
import SocialLink from './SocialLink.astro';
import FocusChip from '../islands/FocusChip';

interface Props {
  firstName: string;
  lastName:  string;
  role:      string;
  tagline:   string;
  location:  string;
  focus:     string;
  socials:   { github: string; linkedin: string; email: string };
}
const { firstName, lastName, role, tagline, location, focus, socials } = Astro.props;
---
<section
  id="top"
  aria-label="Introduction"
  class="hero-section relative isolate
         flex-1 flex flex-col justify-center
         pt-[clamp(72px,9vh,144px)] pb-[clamp(20px,4vh,72px)]"
>
  <div class="relative mx-auto w-full max-w-[1240px]
              px-6 sm:px-10 lg:px-14 hero-rise
              flex flex-col gap-[clamp(16px,2.6vh,36px)]">

    {/* Availability badge — solid green pill, same look as the TopNav and
        About chip so the availability signal stays consistent across the
        page. Wrapped in a link so a click takes the user to #contact. */}
    <div class="flex flex-wrap items-center justify-center lg:justify-start
                gap-x-4 gap-y-2" style="--i: 0;">
      <a href="#contact" class="status-badge">
        <span class="status-badge-dot" aria-hidden="true">
          <span class="status-badge-pulse"></span>
        </span>
        Available for projects
      </a>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-[1fr_minmax(0,280px)]
                gap-x-12 lg:gap-x-16 gap-y-[clamp(20px,3vh,40px)]
                items-start lg:items-start">

      <div class="order-2 lg:order-1 hero-headline-block text-center lg:text-left" style="--i: 1;">
        <h1 class="hero-name font-display text-fg
                   text-[clamp(64px,min(18vw,19vh),260px)]
                   leading-[0.88] tracking-[-0.055em] max-w-[16ch]
                   mx-auto lg:mx-0">
          {firstName} {lastName}.
        </h1>
        <p class="hero-role font-display text-fg-muted
                  text-[clamp(24px,min(3.4vw,4.2vh),44px)]
                  leading-[1.15] tracking-[-0.02em]
                  mt-[clamp(8px,1.2vh,18px)]">
          {role}
        </p>

        <div class="flex flex-wrap items-center justify-center lg:justify-start
                    gap-3 mt-[clamp(12px,1.8vh,22px)]
                    font-mono text-[11px] uppercase tracking-[0.22em] text-fg-faint">
          <span aria-hidden="true">Currently shipping</span>
          <FocusChip
            client:load
            rotationInterval={2600}
            phrases={[
              { text: 'SaaS platforms',      color: '--color-pink'   },
              { text: 'POS systems',         color: '--color-orange' },
              { text: '3D web experiences',  color: '--color-cyan'   },
              { text: 'lead marketplaces',   color: '--color-indigo' },
              { text: 'automation tools',    color: '--color-pink'   },
            ]}
          />
        </div>

        <p
          class="hero-tagline max-w-[54ch] text-[clamp(15.5px,2vh,20px)]
                 leading-[1.6] text-fg-muted
                 mt-[clamp(14px,2.2vh,26px)]
                 mx-auto lg:mx-0"
          set:html={tagline}
        />


        <div class="flex flex-wrap items-center justify-center lg:justify-start
                    gap-3 sm:gap-4 mt-[clamp(16px,2.4vh,28px)]">
          <a href="#work" class="btn-primary group">
            View selected work
            <svg
              xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none"
              stroke="currentColor" stroke-width="1.8" stroke-linecap="round"
              stroke-linejoin="round" class="w-4 h-4 transition-transform
                                             duration-base ease-brand
                                             group-hover:translate-x-0.5"
              aria-hidden="true"
            >
              <line x1="5" y1="12" x2="19" y2="12" />
              <polyline points="12 5 19 12 12 19" />
            </svg>
          </a>
          <a href="#contact" class="btn-secondary">Get in touch</a>

          <span class="hidden sm:inline-block w-px h-6 bg-line mx-1"></span>

          <div class="flex items-center gap-4">
            <SocialLink href={socials.github}            label="GitHub"   icon="github" />
            <SocialLink href={socials.linkedin}          label="LinkedIn" icon="linkedin" />
            <SocialLink href={`mailto:${socials.email}`} label="Email"    icon="mail" />
          </div>
        </div>
      </div>

      {/* Right-side photo — silhouette floats on a single neutral glow, no chrome */}
      <div
        data-portrait-anchor
        class="portrait-stage relative order-1 lg:order-2
               w-full max-w-[clamp(200px,40vw,260px)] lg:max-w-[280px]
               mx-auto lg:mx-0
               lg:-mt-[clamp(8px,3vh,40px)]"
        style="--i: 2;"
      >
        <div class="portrait-glow" aria-hidden="true"></div>
        <img
          src="/profile-withoutbg.webp"
          alt={`Portrait of ${firstName} ${lastName}`}
          width="420"
          height="594"
          loading="eager"
          decoding="async"
          fetchpriority="high"
          class="portrait-img relative block w-full h-auto"
        />
      </div>

    </div>

    {/* Scroll cue — quiet, anchored to the lower-left; hidden on short viewports */}
    <div
      class="scroll-cue hidden lg:flex items-center gap-3 font-mono text-[10.5px]
             uppercase tracking-[0.22em] text-fg-faint"
      style="--i: 5;"
    >
      <span class="block w-10 h-px bg-fg-faint/50 scroll-cue-line"></span>
      Scroll to explore
    </div>

  </div>
</section>

<style>
  /* Headline lockup — heavy name + light role subtitle, both Bricolage Grotesque */
  .hero-name {
    font-weight: 800;
    font-variation-settings: "wdth" 100, "opsz" 96;
    font-feature-settings: "ss01", "ss02", "calt";
  }
  .hero-role {
    font-weight: 700;
    font-variation-settings: "wdth" 100, "opsz" 32;
    font-feature-settings: "ss01", "ss02", "calt";
  }

  /* Inline-bold phrases inside the tagline pick up the brighter foreground
     hue so the bolding actually reads as emphasis, not just a heavier
     weight against the muted body color. */
  .hero-tagline :global(strong) {
    color: var(--color-fg);
    font-weight: 600;
  }

  /* Photo treatment — the source PNG has a transparent background; a single
     soft neutral radial glow sits behind it so the silhouette has somewhere
     to land without competing with the headline. No color, no animation. */
  .portrait-stage {
    isolation: isolate;
  }
  .portrait-glow {
    position: absolute;
    inset: -10%;
    border-radius: 50%;
    background: radial-gradient(
      circle at 50% 50%,
      color-mix(in srgb, var(--color-fg) 8%, transparent) 0%,
      transparent 70%
    );
    filter: blur(60px);
    pointer-events: none;
    z-index: 0;
  }
  :global(.dark) .portrait-glow {
    background: radial-gradient(
      circle at 50% 50%,
      color-mix(in srgb, white 10%, transparent) 0%,
      transparent 70%
    );
  }
  .portrait-img {
    position: relative;
    z-index: 1;
    /* Subtle 10% corner rounding for the top and sides; the bottom dissolves
       into the page via a multi-stop linear fade so the rounded bottom
       corners blend smoothly with the transparency instead of ending hard. */
    border-radius: 10%;
    -webkit-mask-image: linear-gradient(
      to bottom,
      black 0%,
      black 70%,
      rgba(0, 0, 0, 0.78) 82%,
      rgba(0, 0, 0, 0.4)  92%,
      transparent 100%
    );
            mask-image: linear-gradient(
      to bottom,
      black 0%,
      black 70%,
      rgba(0, 0, 0, 0.78) 82%,
      rgba(0, 0, 0, 0.4)  92%,
      transparent 100%
    );
  }

  /* Subtle pulse on the scroll cue's hairline */
  @keyframes scroll-cue {
    0%, 100% { transform: scaleX(1); opacity: 0.5; }
    50%      { transform: scaleX(1.4); opacity: 0.9; }
  }
  .scroll-cue-line {
    transform-origin: left center;
    animation: scroll-cue 2.6s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }

  /* Short-viewport refinement — hide the scroll cue when vertical room is tight */
  @media (max-height: 820px) {
    .scroll-cue { display: none; }
  }
  /* Drop the portrait when there isn't room for it — keeps the headline + CTAs in fold */
  @media (max-height: 720px) {
    [data-portrait-anchor] {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .scroll-cue-line { animation: none; }
  }
</style>
